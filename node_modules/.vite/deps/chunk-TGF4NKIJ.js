import {
  rng,
  runOnEveryParentUntil,
  sleep
} from "./chunk-SJDQO3DF.js";

// node_modules/svelte-typewriter/helpers/typingInterval.js
var typingInterval = async (interval) => sleep(Array.isArray(interval) ? interval[rng(0, interval.length)] : interval);

// node_modules/svelte-typewriter/helpers/writeEffect.js
var writeEffect = async ({ currentNode, text }, options) => {
  runOnEveryParentUntil(currentNode, options.parentElement, (element) => {
    const classToAdd = currentNode === element ? "typing" : "finished-typing";
    element.classList.add(classToAdd);
  });
  for (let index = 0; index <= text.length; index++) {
    const char = text[index];
    char === "<" && (index = text.indexOf(">", index));
    currentNode.innerHTML = text.slice(0, index);
    await typingInterval(options.interval);
  }
};

export {
  typingInterval,
  writeEffect
};
//# sourceMappingURL=chunk-TGF4NKIJ.js.map
